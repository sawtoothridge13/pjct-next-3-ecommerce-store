createOrUpdateComment = createOrUpdateCartProducts

fruitId = productId

comment = productCount

fruitCommentsCookie = cartProductsCookie

fruitComment = cartProduct

fruitComments = allCartProducts

fruitToUpdate = productToUpdate
